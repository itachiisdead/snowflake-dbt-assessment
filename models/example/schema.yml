version: 2

sources:
  - name: tpch
    database: SNOWFLAKE_SAMPLE_DATA
    schema: TPCH_SF1
    tables:
      - name: customer
      - name: orders
      - name: lineitem


models:
  - name: stg_orders
    columns:
      - name: o_orderkey
        tests:
          - not_null
          - unique

          
  - name: customer_revenue
    columns:
      - name: o_custkey
        tests:
          - not_null
          - unique
